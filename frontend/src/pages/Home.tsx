import React from "react";
import { Typography } from "antd";

const Home: React.FC = () => {
  return (
    <div>
      <Typography.Title>
        There are X numbers of companies in the system
      </Typography.Title>
    </div>
  );
};

export default Home;
